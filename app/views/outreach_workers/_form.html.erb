<%= form_with(model: outreach_worker, local: true) do |form| %>
  <% if outreach_worker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outreach_worker.errors.count, "error") %> prohibited this outreach_worker from being saved:</h2>

      <ul>
      <% outreach_worker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :organization %>
    <%= form.text_field :organization %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
